/* Änderungshistorie
* Zweck des Dokuments: Die folgende Komponente wird angezeigt, wenn Drizzle noch nicht vollständig initialisiert ist.
*
* Änderungen:
* Name:
* Datum:
* Änderungen vorgenommen:
*
*/
import React from 'react';
import style from './DrizzleLoading.css';

const DrizzleLoading = ({}) => (
  null
);

export default DrizzleLoading;
